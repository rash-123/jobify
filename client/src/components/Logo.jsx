import logo from "../assets/images/logo.svg";

const Logo = () => {
    return (
        <div>
            <img src={logo} alt='jobify' className='logo'></img>
        </div>
    );
};

export default Logo;
